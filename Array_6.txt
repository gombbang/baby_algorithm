input : int 
뒤집는 과정 : int -> string -> stringBuilder.reverse -> int ? 
	no

	int -> %10 = 최대자리
		/10 != 0
		%10 = 최대자리 * 10 + 해당내용
소수인지 체크하는 방법 : 자기자신보다 작은수로 나뉘어 지지 않으면(약수가 존재하면) 소수로 인정?
	ㅇㅇ
1은 소수로 인정하지 않으므로 if문으로 예외처리
